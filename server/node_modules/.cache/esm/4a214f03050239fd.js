let apiController;_050‍.w("./apiController.js",[["default",["apiController"],function(v){apiController=v}]]);

const express = require("express");
const app = express();
const bodyParser = require("body-parser");
const fs = require("fs");
const mysql = require('mysql');
const cors = require("cors");

const controller = new apiController();




// Port for the API server
const serverPort = 8080;

// Use CORS with express
app.use(bodyParser.json()); // to support JSON-encoded bodies
app.use(cors());
app.use(
	bodyParser.urlencoded({
		// to support URL-encoded bodies
		extended: true
	})
);

app.get("/googleRestaurants", controller.googleRestaurants);
app.get("/getZomatoRestaurants", controller.getZomatoRestaurants);

// Listen on server port
app.listen(serverPort);
console.log(`[Server] API Server running on port: ${serverPort}.`);


